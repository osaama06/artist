# خطة بناء موقع فنانة تشكيلية

**التاريخ:** 2026-08-18

## القرارات النهائية

- **الهدف:** معرض أعمال (Gallery) + مدونة/أخبار ومعارض قادمة
- **الإدارة:** الفنانة نفسها ترفع كل المحتوى عبر لوحة تحكم (Admin Dashboard)
- **اللغة:** عربي فقط، RTL بالكامل
- **البيع:** بدون دفع أونلاين — فقط زر "اطلب هذا العمل" يفتح نموذج استفسار/طلب (البنية جاهزة للتوسع لاحقًا لدفع أونلاين لو احتجنا)
- **الفيديو:** ثانوي، استخدام نادر
- **الهوية البصرية:** جاهزة عند الفنانة (شعار + ألوان) — تُدمج في تصميم الموقع
- **الزوار:** تصفح فقط، بدون تسجيل دخول/تعليقات/نيوزلتر

## 1) الستاك التقني

| الطبقة | التقنية | الدور |
|---|---|---|
| Frontend | Next.js 15 (App Router) + JavaScript (JSX) + Tailwind + shadcn/ui | كل الواجهة، عربي/RTL |
| Backend | Supabase (Postgres + Auth + RLS) | قاعدة البيانات، تسجيل دخول الفنانة، الصلاحيات |
| الوسائط | ImageKit | رفع/تحويل/تسريع الصور والفيديوهات (بدون تخزين وسائط في Supabase) |
| الاستضافة | Vercel (frontend) + Supabase Cloud + ImageKit Cloud | نشر |
| النماذج | react-hook-form + Zod | التحقق من النماذج |
| المحرر | Tiptap (rich text) | كتابة المقالات في لوحة التحكم |

## 2) صفحات الموقع (Public)

1. **الرئيسية** — هيرو بالهوية البصرية، أعمال مميزة، آخر خبر/معرض قادم
2. **المعرض** — شبكة أعمال، فلترة حسب (تصنيف/سنة/خامة)، Lightbox
3. **صفحة العمل الفني** — صورة كبيرة قابلة للتكبير، وصف، خامة، مقاس، سنة، حالة (متوفر/محجوز/مباع)، زر "اطلب هذا العمل"
4. **نبذة عني** — سيرة، صورة شخصية، محطات ومعارض سابقة
5. **المدونة/الأخبار** — مقالات + معارض قادمة (تاريخ ومكان)
6. **تواصل** — نموذج بسيط (اسم/بريد أو جوال/رسالة)

## 3) لوحة تحكم الفنانة (Admin — محمية بـ Supabase Auth)

- **الأعمال الفنية:** CRUD كامل، رفع صور عبر ImageKit، تحديد الحالة (متوفر/محجوز/مباع/غير معروض)
- **المدونة:** كتابة مقالات بمحرر نصوص + صورة غلاف
- **المعارض:** إضافة معرض قادم/سابق (تاريخ، مكان، بوستر)
- **صندوق الطلبات والاستفسارات:** كل طلبات "اطلب هذا العمل" ونموذج التواصل تُجمع هنا مع حالة (جديد/تم الرد)
- **الإعدادات:** تعديل نبذة عني، روابط السوشال ميديا

## 4) تصميم قاعدة البيانات (Supabase — مبدئي)

- `artworks` — title, slug, description, category, medium, dimensions, year, status, price (اختياري), صور (ImageKit)
- `blog_posts` — title, slug, cover_image, content, published_at
- `exhibitions` — title, location, start_date, end_date, poster, upcoming/past
- `inquiries` — name, contact, message, artwork_id (اختياري), type (طلب عمل/تواصل عام), status
- **RLS:** قراءة عامة للمحتوى المنشور فقط، كتابة فقط للفنانة (مسجلة الدخول)، إدخال `inquiries` مسموح للزوار لكن القراءة محصورة بالأدمن

## 5) آلية "طلب لوحة" (بدون دفع)

زر في صفحة العمل → فورم منبثق (اسم، جوال/بريد، رسالة) → يُسجَّل في `inquiries` مرتبطًا بالعمل → يظهر في لوحة التحكم. تحسين مستقبلي اختياري: تنبيه بريد إلكتروني تلقائي للفنانة عند وصول طلب جديد.

## 6) الهوية البصرية المعتمدة — "ليالي الدرعية"

صالة عرض ليلية دافئة: عتمة بنية دافئة (مو أسود نقي) + ذهبي + طيني وردي، كل عمل فني يظهر تحت "ضوئه الخاص" — إحساس فاخر ودافئ بتفاصيل محلية. تفاصيل كاملة وعينات حية (ألوان + خطوط + تخطيط + توقيع بصري تفاعلي) موثّقة في الموكب المعتمد: [artifact: هوية بصرية لموقع الفنانة](https://claude.ai/code/artifact/8c9b2c06-5689-4ed8-851d-386649041fdb).

### الألوان
| الاسم | الهكس | الدور |
|---|---|---|
| عتمة | `#1C1512` | الخلفية الرئيسية |
| سطح | `#241C17` | خلفية البطاقات/الأقسام |
| نص فاتح | `#F3EAE0` | النص الأساسي |
| ذهبي | `#C89B3C` | تمييز أساسي (أزرار، حالات، تفاصيل) |
| طيني وردي | `#8C4A3A` | تمييز ثانوي |
| محايد خافت | `#9C8C7E` | نص ثانوي/وصفي |

### الخطوط (عربي بالكامل، تُحمَّل عبر `next/font/google` في Next.js)
- **العنونة:** Amiri (وزن 700 للعناوين الكبيرة، استخدام مقتصد)
- **النص الأساسي:** Tajawal (وزن 400/700)
- **التسميات/البيانات (مقاس، خامة، سنة):** Cairo (وزن 600، بتباعد أحرف خفيف)

### مفهوم التخطيط
عمل فني بارز بملء الشاشة مع تدرج إضاءة شعاعي (spotlight vignette)، وشريط مصغّرات أفقي تحته يحاكي عرض الشرائح في صالة عرض حقيقية. مساحات هادئة، بدون أي عنصر يزاحم العمل الفني نفسه.

### التوقيع البصري (Signature)
ضوء دافئ يتبع مؤشر الزائر فوق العمل الفني المعروض (radial spotlight)، يحاكي إضاءة صالات العرض الموجّهة — يُعطَّل تلقائيًا مع إعداد "تقليل الحركة" في المتصفح.

## 7) متطلبات ربط المشروع بـ ImageKit (استنادًا لتوثيق ImageKit الرسمي)

المصدر: [imagekit.io/docs/build-with-ai](https://imagekit.io/docs/build-with-ai) + دليل [تكامل Next.js](https://imagekit.io/docs/integration/nextjs) + [قائمة جاهزية الإنتاج](https://imagekit.io/docs/production-checklist).

### أ) إعداد الحساب (قبل الكود)
1. إنشاء/تجهيز حساب ImageKit والحصول من الداشبورد على 3 قيم:
   - `URL Endpoint` (مثال: `https://ik.imagekit.io/your_id`)
   - `Public Key`
   - `Private Key` (سري جدًا — سيرفر فقط، ما يظهر في المتصفح أبدًا)
2. تفعيل **2FA** على حساب ImageKit نفسه (توصية أمان من دليل الإنتاج).
3. تفعيل تنبيهات الفوترة/الاستخدام من الداشبورد.

### ب) التثبيت والإعداد في المشروع (JavaScript)
```bash
npm install @imagekit/next
```

متغيرات البيئة في `.env.local`:
```
NEXT_PUBLIC_IMAGEKIT_URL_ENDPOINT=https://ik.imagekit.io/your_id
IMAGEKIT_PUBLIC_KEY=your_public_key
IMAGEKIT_PRIVATE_KEY=your_private_key   # سيرفر فقط، بدون NEXT_PUBLIC_
```

لف التطبيق بـ `ImageKitProvider` في `app/layout.js`:
```jsx
import { ImageKitProvider } from '@imagekit/next';

export default function RootLayout({ children }) {
  return (
    <ImageKitProvider urlEndpoint={process.env.NEXT_PUBLIC_IMAGEKIT_URL_ENDPOINT}>
      {children}
    </ImageKitProvider>
  );
}
```

### ج) عرض الصور/الفيديوهات
استخدام مكون `Image` من `@imagekit/next` بدل `<img>` العادي في كل الموقع (معرض، صفحة العمل، المدونة) — يفعّل تحويل تلقائي لصيغ حديثة (WebP/AVIF) وأحجام متجاوبة تلقائيًا:
```jsx
import { Image } from '@imagekit/next';

<Image
  src="/artwork-123.jpg"
  width={600}
  height={800}
  alt="اسم العمل الفني"
  transformation={[{ width: 600, height: 800 }]}
/>
```

### د) نقطة API للرفع الآمن (لوحة التحكم فقط)
`app/api/upload-auth/route.js`:
```js
import { getUploadAuthParams } from "@imagekit/next/server";

export async function GET(request) {
  // ⚠️ لازم تتحقق هنا إن المستخدم مسجل دخول كأدمن عبر Supabase Auth
  // قبل ما ترجع بيانات التوقيع — وإلا أي زائر يقدر يرفع ملفات لحسابنا!

  const { token, expire, signature } = getUploadAuthParams({
    privateKey: process.env.IMAGEKIT_PRIVATE_KEY,
    publicKey: process.env.IMAGEKIT_PUBLIC_KEY,
  });

  return Response.json({
    token,
    expire,
    signature,
    publicKey: process.env.IMAGEKIT_PUBLIC_KEY,
  });
}
```

### هـ) مكوّن الرفع (Uploader) داخل لوحة التحكم
```jsx
"use client";
import { upload, ImageKitUploadNetworkError } from "@imagekit/next";
import { useRef, useState } from "react";

export default function ArtworkUploader() {
  const [progress, setProgress] = useState(0);
  const fileInputRef = useRef(null);

  const authenticator = async () => {
    const response = await fetch("/api/upload-auth");
    return response.json();
  };

  const handleUpload = async () => {
    const file = fileInputRef.current?.files?.[0];
    if (!file) return;
    const { signature, expire, token, publicKey } = await authenticator();

    try {
      const result = await upload({
        file,
        fileName: file.name,
        signature,
        expire,
        token,
        publicKey,
        onProgress: (e) => setProgress((e.loaded / e.total) * 100),
      });
      // result.url / result.fileId → نخزّنه في جدول artworks بـ Supabase
    } catch (error) {
      if (error instanceof ImageKitUploadNetworkError) {
        console.error("خطأ شبكة أثناء الرفع:", error.message);
      }
    }
  };

  return (
    <>
      <input type="file" ref={fileInputRef} accept="image/*,video/*" />
      <button onClick={handleUpload}>رفع</button>
      <progress value={progress} max={100}></progress>
    </>
  );
}
```
بعد نجاح الرفع، نخزّن `url`/`fileId` اللي يرجعه ImageKit داخل صف `artworks` أو `blog_posts` أو `exhibitions` في Supabase — Supabase ما يخزّن أي ملف وسائط، بس يحفظ الرابط.

### و) الفيديو (استخدام نادر حسب قرارك)
نفس مكون `Image`/`<video>` مع رابط ImageKit المحوَّل. لو صار فيديو أطول من 30 ثانية، نفعّل **Adaptive Bitrate Streaming** من إعدادات ImageKit (زي يوتيوب — يتكيف مع سرعة نت الزائر).

### ز) توصيات جاهزية الإنتاج (من دليل ImageKit)
- تفعيل تحويل الصيغ الحديثة تلقائيًا (WebP/AVIF)
- عدم رفع جودة الصورة فوق 90% أو الفيديو فوق 70% (يكبّر الحجم بدون فرق يُذكر)
- Lazy loading لأي صورة خارج الشاشة
- تحديد الحد الأقصى لـ DPR بـ 2 لموازنة الجودة/الحجم على الجوال
- لا حاجة لأي تعديل في `next.config.js`

### ح) أدوات اختيارية للتطوير بمساعدة AI (مستقبلًا)
ImageKit توفر MCP servers لو حبينا أنا (Claude) أتفاعل مباشرة مع حساب ImageKit أثناء التطوير:
- **DevTools MCP** (توثيق/مساعدة عامة، بدون مصادقة): `https://devtools-mcp.imagekit.io/mcp`
- **API MCP** (رفع/بحث/تنظيم وسائط فعلي من الحساب، يحتاج OAuth): `https://api-mcp.imagekit.io/mcp`

## 8) مراحل التنفيذ (Roadmap)

| المرحلة | المحتوى |
|---|---|
| 0 | إعداد المشروع + ربط Supabase وImageKit + تركيب الهوية البصرية بـ Tailwind |
| 1 | الموقع العام: الرئيسية، المعرض، صفحة العمل، نبذة، تواصل |
| 2 | لوحة التحكم: تسجيل دخول + إدارة الأعمال الفنية |
| 3 | المدونة + المعارض القادمة |
| 4 | نظام طلب الأعمال + صندوق الاستفسارات |
| 5 | تحسين الأداء وSEO وفحص RTL/الاستجابة على الجوال |
| 6 | النشر النهائي وتسليم دليل استخدام بسيط للفنانة |

## الحالة

📌 **المرحلة 0 والمرحلة 1 والمرحلة 2 مكتملة (2026-08-18).**

### المرحلة 2
- ✅ تسجيل دخول الفنانة عبر Supabase Auth (بريد/كلمة مرور) — `/admin/login`
- ✅ حماية كل مسارات `/admin/*` على مستويين: `site/src/proxy.js` (يعيد التوجيه قبل حتى ما توصل الصفحة) + فحص إضافي داخل كل Server Action وداخل `layout.js` للوحة (دفاع بعمق، لو تغيّر الـ matcher بالغلط)
- ✅ **ملاحظة تقنية مهمة:** Next.js 16 غيّر اسم `middleware.js` إلى **`proxy.js`** (نفس الوظيفة، تحذير مكتوب في توثيق النسخة المثبّتة). لو أي كود أو مثال قديم يذكر "middleware"، الصحيح بهذا المشروع هو `proxy.js` + `export function proxy(...)`.
- ✅ رفع الصور الحقيقي شغّال: `/api/upload-auth` (موقّع ImageKit، محمي بفحص تسجيل الدخول) + مكوّن رفع (`ImageUploader`) يرفع من المتصفح مباشرة لـ ImageKit ويخزّن مسار الملف
- ✅ **إدارة الأعمال الفنية كاملة:** قائمة + إضافة + تعديل + حذف (`/admin/artworks`)، مع كل الحقول (العنوان، الرابط، الوصف، التصنيف، الخامة، المقاس، السنة، السعر، حالة البيع، صورة الغلاف، منشور/مميز)
- ✅ **صندوق الاستفسارات** (`/admin/inquiries`) — عرض كل الطلبات مع بيانات المتواصل والعمل المرتبط، وتبديل الحالة (جديد ↔ تم الرد)
- ✅ **الإعدادات** (`/admin/settings`) — تعديل النبذة، الصورة الشخصية، روابط التواصل الاجتماعي (تنعكس مباشرة على صفحة "نبذة عني" العامة)
- ✅ فصلنا الواجهة العامة عن لوحة التحكم عبر route group‏ `(site)` — الآن `/admin/*` ما يطلع فيها الهيدر/الفوتر العام
- ✅ تحقّق: بنينا المشروع production build كامل (`next build`) للتأكد إن كل صفحات الأدمن تترجم بدون أخطاء (لأن صفحات الأدمن تعيد التوجيه في التطوير قبل ما تترجم، ما ينكشف فيها أخطاء إلا بالبناء الكامل) — نجح بدون أي خطأ
- ⚠️ **لم يتم إنشاء حساب الفنارة نفسه** — بما إنه إدخال كلمات مرور/إنشاء حسابات شيء ما أقدر أسويه بالنيابة عن المستخدم (قاعدة أمان)، لازم الفنانة أو أوسامة يسوّونه يدويًا من [لوحة Supabase](https://supabase.com/dashboard/project/gyieuvrnixlolsfpefcg/auth/users) → Add User → إدخال البريد وكلمة مرور. بعدها تسجيل الدخول والتجربة الفعلية للوحة التحكم لم تُختبر حيًا (بس البنية والمسارات كلها متحقق منها بالبناء الكامل)

**التالي:** المرحلة 3 — إدارة المدونة والمعارض من لوحة التحكم (نفس نمط CRUD المستخدم بالأعمال الفنية، تطبيقه أسرع لأنه نفس القالب). ملاحظة: استخدمنا `textarea` عادي للمقالات بدل محرر Tiptap المذكور بالخطة الأصلية — تبسيط مقصود، ممكن نرقّيه لاحقًا لو احتجنا تنسيق أغنى (عناوين فرعية، صور داخل النص).

### المرحلة 3 مكتملة (2026-08-21)

- ✅ `/admin/blog` — إضافة/تعديل/حذف مقالات، مع رفع صورة غلاف حقيقي ورابط (slug) بنفس قاعدة التحقق المستخدمة بالأعمال الفنية
- ✅ `/admin/exhibitions` — إضافة/تعديل/حذف معارض، تاريخ بداية/نهاية، بوستر، وصف
- ✅ لوحة "نظرة عامة" صارت تعرض عدد كل الأقسام الأربعة (أعمال، استفسارات جديدة، مقالات، معارض)
- 🔧 لقينا وصلّحنا أثناء البناء: صفحة المدونة العامة كانت تعرض صورة غلاف المقال بس ما تعرض بوستر المعرض أبدًا (حقل موجود بقاعدة البيانات وما له أي استخدام بالواجهة) — صار يظهر الحين

### التصنيفات (إضافة غير مخطط لها بالأصل، 2026-08-21)

طلب المستخدم إمكانية إضافة وحذف تصنيفات الأعمال بشكل مستقل (كانت نص حر داخل كل عمل). حولناها لجدول تصنيفات حقيقي مربوط بالأعمال، مع صفحة `/admin/categories` (إضافة + حذف مع عدد الأعمال بكل تصنيف). حذف تصنيف لا يحذف الأعمال المرتبطة فيه، بس يشيل عنها التصنيف.

## وين وصلنا الحين

المراحل 0-4 كلها مكتملة. الباقي من الخطة الأصلية:
- **المرحلة 5**: الأداء وSEO وفحص الجوال — ما سويناها بعد
- **المرحلة 6**: النشر الفعلي (الموقع لسا يشتغل محليًا بس، ما فيه رابط عام) + دليل استخدام للفنانة — ما سويناها بعد

سُئل المستخدم عن الأولوية بين إكمال المحتوى (مدونة/معارض) والنشر والفحص الشامل — اختار إكمال المحتوى أول (هذا اللي صار بالمرحلة 3 أعلاه). القرار بين النشر والفحص الشامل لسا ما تحدد.

### إصلاحات بعد أول استخدام فعلي من الفنانة (2026-08-18، نفس اليوم)

الفنانة سجّلت دخول فعليًا وأضافت أول عمل حقيقي ("المدينه الحديثه") — وصادفت مشكلتين، تم إصلاحهما:

1. **"نوت فاوند" عند فتح العمل الجديد:** سبب تقني بحت — Next.js 16 ما يفك ترميز رابط الـ slug بشكل ثابت لما يكون عربي (أحيانًا يوصل مشفّر `%D8%A7...` بدل النص العربي الفعلي، تأكدت منه بالتجربة المباشرة). الحل: [`safeDecodeSlug`](site/src/lib/data.js) يطبّق `decodeURIComponent` دائمًا (آمن حتى لو كان النص مفكوك أصلاً) قبل أي بحث بقاعدة البيانات — مطبّق بصفحة العمل الفني وصفحة المقال. غيّرت أيضًا slug العمل نفسه من عربي إلى `al-madina-al-haditha` (الرابط بس، العنوان ما تغيّر) عشان يتوافق مع قاعدة التحقق الجديدة بالخطوة التالية.
2. **الصورة ما ظهرت:** الصورة المرفوعة ما وصلت فعليًا لحقل `cover_image_url` — الاحتمال الأقوى إنه تم الضغط على "حفظ" قبل ما يخلص الرفع (ما كان فيه شيء يمنع هذا). الحل: [`ImageUploader`](site/src/components/admin/ImageUploader.js) الآن يوقف زر "حفظ" فعليًا أثناء الرفع (يتحول لـ"بانتظار اكتمال رفع الصورة…" وما يقبل الضغط) بدل ما يكون تحذير نصي بس.
3. **وقاية إضافية:** حقل الـ slug بنموذج العمل الفني الآن يرفض غير الحروف الإنجليزية الصغيرة/الأرقام/الشرطات (HTML `pattern`)، عشان ما تتكرر مشكلة الـ slug العربي مستقبلًا.

تم التحقق من الإصلاحات الثلاثة حيًا (بما فيها إعادة بناء production build نظيف بدون أخطاء).

### إصلاح جذري إضافي — مشكلة الصور لم تكن منتهية فعليًا (2026-08-20)

الإصلاح السابق (تعطيل زر الحفظ أثناء الرفع) كان تخمينًا معقولًا لكن غير كافٍ — استمرت المشكلة. السبب الحقيقي: حقل الصورة المخفي بمكوّن [`ImageUploader`](site/src/components/admin/ImageUploader.js) كان "غير محكوم" (uncontrolled) بلغة React — يُملأ عبر `ref.current.value = ...` بعد الرفع، وهذا التعديل المباشر على الـ DOM ما كان يوصل بثبات لبيانات الفورم وقت الحفظ الفعلي عبر Server Action (تأكدنا منها بتجربة معزولة مؤقتة لا تحتاج تسجيل دخول: قيمة محفوظة بـ React state تصل دائمًا، قيمة محفوظة بـ ref فقط لا تصل بثبات).

**الحل:** تحويل الحقل المخفي ليكون "محكوم" فعليًا (`value` من React state بدل `defaultValue` + ref) — هذا يضمن توافق الفورم مع آخر قيمة مرفوعة دائمًا.

**تحقّقنا من كل شي بالبيانات الفعلية** (بدون تخمين): استخدمنا ImageKit Media API مباشرة (بدون أي تسجيل دخول) عشان نتأكد الصور فعلاً وصلت لـ ImageKit، وقارناها بتوقيت تحديث كل عمل بقاعدة البيانات — أثبتنا إن 5 صور انرفعت بنجاح لكن ما ارتبطت بأي عمل. ربطناها يدويًا بالعمل الصحيح حسب التوقيت، فكل الأعمال (عدا اثنين ما حاولت ترفع لهم صور بعد) صارت تعرض صورها الحقيقية فورًا بدون أي إعادة رفع من الفنانة.

---

### الحالة قبل المرحلة 2 (سجل تاريخي)

### المرحلة 0
- ✅ مشروع Next.js (JavaScript) منشأ في مجلد [`site/`](site/) — App Router + Tailwind v4 + ESLint
- ✅ حزمة `@imagekit/next` مثبّتة و`ImageKitProvider` مربوط في [`site/src/app/layout.js`](site/src/app/layout.js)
- ✅ الهوية البصرية "ليالي الدرعية" مطبّقة فعليًا: ألوان + خطوط (Amiri/Tajawal/Cairo) في [`site/src/app/globals.css`](site/src/app/globals.css)
- ✅ RTL + `lang="ar"` مفعّلة على مستوى الصفحة كاملة
- ✅ `.env.local` منقول لمجلد `site/` ومحمي بـ `.gitignore`

### المرحلة 1
- ✅ قاعدة بيانات Supabase أُنشئت فعليًا: جداول `artworks`, `blog_posts`, `exhibitions`, `inquiries`, `site_settings` — كلها بـ RLS مفعّل (قراءة عامة للمنشور فقط، كتابة للمسجّلين دخول فقط، `inquiries` تقبل إدخال من الزوار وقراءة للأدمن فقط). تم فحص Advisors (أمان + أداء) بدون تحذيرات مهمة.
- ✅ عميل Supabase مربوط بالمشروع (`site/src/lib/supabase/server.js` و`client.js`) عبر `@supabase/ssr`
- ✅ بيانات تجريبية (seed) مُدخلة يدويًا للتحقق من الصفحات: 6 أعمال فنية، معرضان (قادم وسابق)، نبذة تعريفية مبدئية — **كل هذا محتوى مؤقت وسيُستبدل بمحتوى حقيقي عبر لوحة التحكم في المرحلة 2**
- ✅ الصفحات العامة الفعلية مبنية ومتصلة ببيانات Supabase حية:
  - الرئيسية (`/`) — هيرو بالعمل المميز مع تأثير "الضوء المتتبع للمؤشر" (spotlight)، شريط أعمال مختارة، تنبيه المعرض القادم
  - المعرض (`/gallery`) — شبكة أعمال + فلترة حسب التصنيف
  - صفحة العمل الفني (`/gallery/[slug]`) — تفاصيل كاملة + نموذج "اطلب هذا العمل" (يظهر فقط للأعمال المتوفرة)
  - نبذة عني (`/about`) — السيرة من `site_settings` + المعارض السابقة
  - المدونة (`/blog` و`/blog/[slug]`) — المعارض القادمة + المقالات المنشورة (فارغة حاليًا لعدم وجود مقالات)
  - تواصل (`/contact`) — نفس نموذج الطلب بنوع "تواصل عام"
- ✅ نظام الطلب/الاستفسار (**المرحلة 4 من الخطة الأصلية**) اشتغل فعليًا قبل موعده — Server Action واحد (`site/src/app/actions.js`) يُدخل الصفوف في `inquiries`، مُستخدَم من صفحة العمل الفني وصفحة تواصل، تم اختباره حيًا (إدخال ✅، حذف تجريبي بعدها)
- ✅ تحقّق كامل عبر معاينة حية: كل الصفحات تعمل بدون أخطاء console، RTL سليم، حالات البيع (متوفر/محجوز/مباع) تتحكم بظهور زر الطلب بشكل صحيح
- ⚠️ ملاحظة تقنية: تشغيل معاينتين لنفس المشروع بالتوازي (من جلستين) يحتاج منفذ (port) مختلف + `distDir` مختلف في `next.config.mjs` (أضفنا `NEXT_DEV_PREVIEW=1` كخيار عبر سكربت `npm run dev:preview`) لأن Next.js يمنع تشغيل خادمين على نفس مجلد `.next`

**التالي:** المرحلة 2 — لوحة تحكم الفنانة (تسجيل دخول عبر Supabase Auth + CRUD للأعمال الفنية مع رفع صور حقيقي عبر ImageKit) لتستبدل بيانات الـ seed التجريبية بمحتوى حقيقي.
