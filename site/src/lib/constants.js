// Business contact info — kept in one place since it may end up used from
// more than one page (currently: the "buy this artwork" button).
export const WHATSAPP_NUMBER = "966533812602"; // international format, no leading 0 or +
